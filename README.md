# Code Review Assistant - Sonar Linter

```yaml
    - name: SonarLinter
      uses: OSFGlobal/CodeReviewAssistant-SonarLinter@master
      with:
          azureAccount: ${{ secrets.AZURE_ACCOUNT }}
          azureKey: ${{ secrets.AZURE_KEY }}
```